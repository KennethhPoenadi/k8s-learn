apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sikp-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/KennethhPoenadi/k8s-learn.git
    targetRevision: main
    path: k8s
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: sikp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
